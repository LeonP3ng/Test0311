<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bluemsun.demo.dao.UserdaoInterface">

    <select id="checkStudentLogin" resultType="Student">
        SELECT * from student where student_id = #{studentId} and password = #{password}
    </select>

    <select id="getStudentInfo" resultType="Student">
        SELECT * from student where student_id = #{studentId}
    </select>

</mapper>
